package com.kh.app.member.service;

/*
public class MemberService {

    private MemberDao memberDao = new MemberDao();

    public void join(MemberModel memberModel) throws SQLException {
        // 회원 있는지 검사
        MemberModel existingMember = memberDao.getMemberByid(memberModel.getUserId());
        if (existingMember != null){
            throw new SQLException("회원이 이미 있어요. /n/ 입력하신 아이디:" + memberModel.getUserId());
        }

        // 새로운 회원 회원등록함
        MemberDao.insertMember(member);
    }

    public MemberModel login(String id, String pwd) throws SQLException{
        //아이디 패스워드 입력하고 회원 찾기
        MemberModel member = MemberDao.getMemberByidAndPwD
    }



    //회원가입


    }

*/
